name: meta_data
description: "A meta parser."
version: 1.0.0-beta.3
homepage:

resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
